inputfile File;
inputfile Filenotfound;
int main(){
const NUMBER_OF_LINES = 2309;
  file = newfile("words.txt")
  Scanner scanner = new Scanner(file);

  for (int i = 1; i <= lineNumber -1; i++)

string answer = scanner.nextLine();
cout << "The word is " << answer << endl;

scanner.close()

for (int o = 0; o < 5; o++)
  {
    cout << "Please enter your guess\n"
  }
if (guess == answer0
    cout << "YOU WIN!" << endl;
for (int i = 0; i < guess.length(); i++
  char guesschar
  char answerchar

  if (guessChar == answerChar)
  {
  cout << "Character " guesChar " at index " i " is correct" <<endl;
  }
    else if (answer!= -1
    {
    cout << "Character " guessChar " at index " i
